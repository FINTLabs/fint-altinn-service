fint:
  kafka:
    topic:
      org-id: ${fint.org-id}
      domain-context: altinn
    application-id: ${fint.application-id}
    default-replicas: 1
spring:
  kafka:
    consumer:
      group-id: ${fint.application-id}